import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        alignItems: 'center',
    },
    menuContainer: {
        width: 8 * 24,
    },
    menuIcon: {
        marginHorizontal: 13
    },
    menuItem: {
        fontSize: 16
    },
})

export default styles